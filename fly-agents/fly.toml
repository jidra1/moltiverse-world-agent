app = "moltirealm-agents"
primary_region = "iad"

[build]
  dockerfile = "../Dockerfile.agents"

[env]
  API_URL = "https://moltirealm.up.railway.app"

[[vm]]
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1
